import { ArchivoDirective } from './archivo.directive';

describe('ArchivoDirective', () => {
  it('should create an instance', () => {
    const directive = new ArchivoDirective();
    expect(directive).toBeTruthy();
  });
});
