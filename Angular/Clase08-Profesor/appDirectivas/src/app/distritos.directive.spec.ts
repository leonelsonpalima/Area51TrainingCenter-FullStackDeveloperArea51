import { DistritosDirective } from './distritos.directive';

describe('DistritosDirective', () => {
  it('should create an instance', () => {
    const directive = new DistritosDirective();
    expect(directive).toBeTruthy();
  });
});
